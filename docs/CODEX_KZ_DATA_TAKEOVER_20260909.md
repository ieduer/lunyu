## 2026-09-09 KZ completion rate-limit recovery — local verified, release pending

Purpose `20260909-kz-rate-limit-recovery` is independent of the accepted catalogue correction and the still-open fleet goal. Both current UC6cbc exceptions at12:31:34/42Z were authenticated KZ minute-limit rejections with complete terminal diagnostics. Exact trace joins are in `uc-kz-rpc-rejection-classification.json`; this does not establish the historical missing-terminal root cause.

The actual KzGrowthEvidence caller can opt into a finite429 result using `typedCompletionRateLimit:true`. Other callers, old KZ requests without opt-in, success receipts and unexpected errors retain their behavior. No rate cap, schema, identity, score, resourceKey, source activation, corpus or evidence is changed. KZ maps both the finite result and predecessor's fixed rate errors to429 with a bounded Retry-After. Manual retry uses the same event nonce after same-tab reload; opening a pending chapter restores the button. Waiting and reopening send no completion request. Source rejection writes no My projection. Partial central synchronization remains visibly retryable. No automatic retry, AI retry or stored credentials.

Verified Node24.18.0: UC89 focused tests and growth checks;616 ordinary tests pass;60 support pass/one existing skip; strict4422218-byte build SHA256 `5569a84e328ef95e9af665dd833805fc0794ec7fe48304119c29566dc022c8be`. KZ full `npm run check` passes including7 new recovery tests and all existing catalogue/contract checks. Two current runtime-byte pins were updated only after exact all-other-index-byte and scoring/auth/registry/transaction comparisons; immutable historical candidate sources were not changed. Local verification is not production or real-user acceptance.

Current production and rollback for this repair: UC `6cbc3f62-6440-4d44-a3fa-36be924580e6@100`, deployment `290511ed-7cf0-4ef5-9e03-7e866b06b9e0`; KZ Pages `0001fce8-9002-4ed6-8d5e-be11200597bc`, source `cc5468b7ba1483d5d78f1e92458f57b0b0305b0a`. Preserve forward D1/browser history and all bindings during either code rollback. Do not use earlier catalogue rollback anchors for this new purpose. Fresh readback23:19/23:21Z:149 hub relations,100 Workers,39 Pages; KZ production and preview both bind only GROWTH_EVIDENCE→bdfz-user-center/KzGrowthEvidence. Git main push triggers KZ production; branch push triggers preview. Do not add a second publisher.

Release sequence: clean published UC main with reviewed private control, exact0/1/5/100 stages and same-candidate checks; KZ branch preview static/source checks followed by reviewed main push and exact Pages readback. Both source versions interoperate during rollout and rollback. Actual normal annotation→source receipt→My projection→ordinary reload must still be measured. Do not manufacture a minute-limit burst or use health200 as acceptance. Future natural429/error observation remains independently required for causal error-reduction claims.

Evidence authority: `/Users/ylsuen/CF/_meta/reports/operations/.my-architecture-serial7-20260909`, especially `uc-kz-rate-limit-fanout-reviewed.json`, `uc-kz-rate-limit-source-boundary-review.json`, `uc-kz-rate-limit-local-verification-v2.json`, `kz-rate-limit-full-check-v2.json` and `kz-rate-limit-production-preflight.json`. The collector completed its JSON but refused an existing predecessor Markdown name; the reviewed derivative documents this bounded local output error without overwriting historical evidence. E7 is retain_hot through review2026-09-16. One existing object store per repo; derivatives are registered in the root runtime manifest and removed after release recovery publication. No-new-capability: existing Workers RPC, Pages, sessionStorage and fixed status responses; no new dependency, cost tier, capability, credential or remote data resource.

Claude continuation: the541 IDs,297 corrected titles,29 display aliases and512 visible entries remain exactly as accepted. This new repair concerns transient synchronization only; do not renumber, delete or rebuild the corpus to address rate limits.

## 2026-09-09 — KZ data repair and My acceptance completed for this scope

Codex owns `20260909-kz-progress-identity`, `20260909-kz-catalogue-compatibility` and the separate central-label transaction `20260909-uc-kz-catalogue-presentation`. The paused Claude KZ investigation had not changed corpus/code. Claude should use this record before resuming and must not delete or renumber legacy chapters.

Accepted functional KZ release: Pages `70e81a38-db84-4afd-9eb9-565954830742`, source `235715c71484c852e0898061f211c9627a7610e3`. Immediate leaf rollback: Pages `22a5e555-b804-4b58-a988-7d4274cd575a`, source `cf5aeffe3dfb3f3dfc2b28e521d015866148b20d`. A later documentation-only Pages build must preserve all runtime asset hashes; its exact control-plane readback is recorded in the root evidence/ledger and current local state. Never infer a live deployment ID from a Git commit alone.

The repair preserves all541 numericIDs in originalorder and all1623 text/translation/annotation values. It corrects297 titles and declares29 display aliases: menus and random selection show512 canonical entries, while old29 IDs remain loadable and separately identifiable. It also restores savedread markers when menus are created and reads server progress from `meta`. No local or central history was cleared, renumbered or converted to another resourceKey. Genuine equaltext at different coordinates remains separate.

Current admission remains `kz-79b9c5647108bc9b`, itemCount541, resourceKeySha256 `79b9c5647108bc9be8fea0f23e9585cff24e0083e70cadbaf52629e54fdfe6ca`, threshold163. CorpusSHA256 `8c9ebc18d6c6f0c156cad89aaab89f6a413cab0f9b2fa2fa7b65ac2b0d4400b1`; manifestfileSHA256 `8c54750308b3d05b931e4eeff557536610cce07da98f8e8134c514c629cf2048`. `sourceSha256` is provenance for the changed content bytes; it is not this current stable admission-key digest. Do not reuse this conclusion blindly for YW's different evidence contract.

Real authenticated acceptance passed. Original539/540/541 history survived the progress repair; chapter539 normal completion reached My and bothsites afterreload. The later genuine193 normalannotation action produced source200 and normalcoupledAI200 at07:48Z; no syntheticcompletion or oldrecord replay. Originalfour localIDs retained their exacthash, and adding193 yieldedfive. After the separate UC0/1/5/100 release and ordinary09:15 reload, allfour central evidence identities/references/counts/progress/timestamps and completeownerscore remainexact. My displays 泰伯8.5 and 堯曰20.1/20.2/20.3; the visible193card hasonequalifiedevidence and100% progress. UC currentacceptedruntime is `6cbc3f62-6440-4d44-a3fa-36be924580e6@100`, deployment `290511ed-7cf0-4ef5-9e03-7e866b06b9e0`, source3adc9ac, separateUCrollbacka66. Its snapshot changed onlytitles/groups; all541 identities and scoring remainfixed.

Changed KZ source/test files: `data/dialogues.json`, `data/learning-manifest.json`, `assets/js/app.js`, `scripts/build_learning_manifest.mjs`, `scripts/test_progress_restore.mjs`, `scripts/test_chapter_catalogue.mjs`, `package.json`. Source gates passed19namedtests plus3existing assertion suites,22corpus and15manifest negativecases; rootchecked every541completionmetadata contract and1623 unchangedcontent values. Run the eightpoint `docs/VERIFICATION_STANDARD.md`. PinNode24.18.0; no new capability, dependencyinstall, credentials, notification or source/backend binding change.

Evidence authority: `/Users/ylsuen/CF/_meta/reports/operations/.my-architecture-serial7-20260909`; see `kz-catalogue-production-readback.json`, `kz-catalogue-real-source-result.json`, `kz-catalogue-preserved-history.json`, `uc-kz-title-stage-100-acceptance.json` and `uc-kz-title-ui-baseline-adjudication.json`. Ownercodex-my-architecture-serial7, retain_hot, review2026-09-16. The privateoriginal UIbaseline hashed empty hidden text and is explicitly invalid for referenceidentity; authenticated08:22baseline plus actualnonempty09:15UI prove preservation. The failed/lagging telemetry and browser-recovery records remain available. Repositorysource is recoverable from the sole existingGit objectstore with `git worktree add --detach <ABSENT_REGISTERED_WORKTREE> 235715c71484c852e0898061f211c9627a7610e3`, followed by pinnedmanifest/test/hash checks; preserve forward learnerrecords when rolling back code.

Separate unfinished purposes: APISprimarytimeouts andslot6 isolation; current541/163 centralassessment versus512displayentries and separatelycreditablelegacyaliases; originalUCmissingterminal and otherMy/fleet incidents. A single normalAI200 does not prove providerpool recovery. This record closes the scoped title/duplicate-display/progress defect, not those independent policies or the overall task. Claude's otherwork should be taken over only under the user's remaining quota condition, after the overall My/fleet goal.

## Historical entries (superseded where they conflict with the current entry)

## 2026-09-09 — Catalogue compatibility repair ready for staged release

Ownercodex-my-architecture-serial7; change20260909-kz-catalogue-compatibility. Live predecessor is Pages22a5e555-b804-4b58-a988-7d4274cd575a / sourcecf5aeffe3dfb3f3dfc2b28e521d015866148b20d, also the immediate rollback for this data/presentation repair. The earlier progress-restoration release remains accepted. This catalogue version is not yet deployed.

The corpus retains all541 original numeric IDs in original order and all1623 text/translation/annotation values. Correct297 display titles, including chapter193→泰伯8.5; add29 explicit displayAliasOf links for actual duplicate coordinates. Navigation and random selection show512 canonical entries; legacy29 IDs remain loadable and old local/central evidence is retained. Restoring a legacy alias may select its canonical display entry, but never writes a new canonical completion or transfers credit. Equal text at different legitimate coordinates, including3/493, stays separate.

Current User Center admission remains manifestVersionkz-79b9c5647108bc9b, resourceKeySha25679b9c5647108bc9be8fea0f23e9585cff24e0083e70cadbaf52629e54fdfe6ca, itemCount541 and threshold163. SourceSha256 changes to8c9ebc18d6c6f0c156cad89aaab89f6a413cab0f9b2fa2fa7b65ac2b0d4400b1 because titles/aliases changed; it is provenance, not this admission-key digest. Manifest fileSHA8c54750308b3d05b931e4eeff557536610cce07da98f8e8134c514c629cf2048. Central static presentation titles may remain historical; no central snapshot rewrite is included. The central541 denominator and separately creditable legacy alias IDs remain an explicit independent policy issue, not silently changed to512.

Independent root review checks all541 actual SDK completion metadata objects against currentUCstaticresources and exactsourcehandler fixtures, with1623 unchangedcontent comparisons and29explicit aliases; allpass withzero network/studentwrites. Existing pinnedNode24.18 checks:19namedtests plus3existingassertion suites,22corpus and15manifest negativecases passed. Four source hashes still match reviewed E7/kz-data-root-review.json after the YW incident pause. These are source readiness, not production acceptance.

Release: publish the tested branch codex/kz-catalogue-compatibility-20260909, verify exact Pages preview, then publishmain after source/config/rollback gates. Fresh07:30Z project readback now lists GROWTH_EVIDENCE on both production and preview; the earlier04:29Z preview inventory had none. This is current configuration evidence, not authenticated preview acceptance. Pages preview does not share the normal bdfz.net login origin; real central acceptance remains on the actual product path. Production will verify preserved owner storagehash (fourstoredIDs,1a79664b1ad331a835e771c571a0e96bc25081a5c6375a989e1e635d2b2ffcf0), visible old readmarkers, a genuine new normalreading→My→reload, correctedmenu/legacyalias behavior and unchanged541contract. Do not replay prior539/540/541 completions. The existing normalannotationbutton necessarily starts the sharedAI request after sourcecompletion; acceptance must observe that genuine behavior (at most3existingproductattempts), without intercepting it or manufacturing a sourcewrite. It is not a separate operational/model probe. Existing APIS503/timeouts andslot6 admission issue remain independentlyopen.

Source/test changes are data/dialogues.json, data/learning-manifest.json, assets/js/app.js, scripts/build_learning_manifest.mjs, scripts/test_progress_restore.mjs, scripts/test_chapter_catalogue.mjs andpackage.json. Backendfunctions/identity/APIS/cookies/registration/Appbridge/credentials unchanged. CAPABILITY_FIT=no-new-capability. Source and build inputs remain Git-backed under ieduer/lunyu; recover predecessor into an absent manifest-registeredworktree with `git worktree add --detach <ABSENT_WORKTREE> cf5aeffe3dfb3f3dfc2b28e521d015866148b20d`, then runpinnedNodechecks and verifythe recordedcorpus/manifest before release. No D1/KV rewrite or datarestore is needed or authorized.

Evidence authority `/Users/ylsuen/CF/_meta/reports/operations/.my-architecture-serial7-20260909`: kz-data-implementation.json, kz-data-root-review.json, kz-catalogue-fanout-freeze.json andkz-catalogue-preflight-current.json. Source/evidence retain_hot, ownerroot, review2026-09-16 until acceptedrecovery/supersedingrecord; no deletion atreviewdate. Existingworktree isroot-owned derivative with exactcleanup pending. Canonicalabf77e8 and dirtymanuals arepreserved; releasebranch/sourcepins arecurrent authority.

Claude takeover: do not delete duplicates, renumberresource IDs, overwrite progress or change541/163 to512. This implementation corrects display data while retaining learning identity. Update this note with deployedversion andactualhistoryreadback before any further corpuswork. The earlier dated proposal below is historical; the implemented choice retains541rawrows with29aliases, rather than deleting rawrows.

## 2026-09-09 04:50Z — KZ reading restoration accepted; separate data and provider issues open

Production Pages `22a5e555-b804-4b58-a988-7d4274cd575a`, runtime source `cf5aeffe3dfb3f3dfc2b28e521d015866148b20d`, exact app SHA256 `205006667da7a191455b61ebc29d0113cf54d84a8b5348fb73a1cd9cfe2e94b2`. Production binding/date and all541 corpus/manifest identities are unchanged. Immediate code rollback is Pages `9d181d0a-60cb-463d-8421-594ed2c0eea9`; preserve actual owner forward evidence.

The prior owner20.2/20.3 records remained complete in My, and both menu markers now restore after reload. One new normal owner annotation reveal for public20.1 / immutable `chapter-539` returned an exact source receipt (HTTP200, ok, kz, expected resource and manifest). My post-reload cards increased10→11, KZ2→3; selected20.1 has1 qualified `site_progress / authenticated_product_activity` reference and100% completion. KZ post-reload539/540/541 markers all true. Passive observer removed by reload. This specific source completion → central projection → both reload path passes; no replay, synthetic completion, credential change or message/notification was used. Note: E6's public-coordinate label chapter-20.2 corresponds to immutable chapter-540; do not reuse that label as an API key.

The same ordinary AI interaction first returned503 and then200 after the product's automatic retry. Fresh APIS diagnostics show primary3.6 timeout, then slot6 fallback401; retry used a different fallback key. This is an unresolved real provider-path error, not a green full-product/fleet conclusion. Whole My/fleet objective remains incomplete. Next independent action isolates slot6; KZ title normalization and29 display-only aliases remain unapplied, with central541/163 policy frozen. Read docs/CODEX_KZ_DATA_TAKEOVER_20260909.md before Claude resumes.

Evidence: `/Users/ylsuen/CF/_meta/reports/operations/.my-architecture-serial7-20260909/` (`kz-production-readback.json`, `kz-new-source-receipt.json`, `my-kz-qualified-evidence.json`, `kz-postreload-completion.json`, `kz-new-product-result.json`, `kz-apis-503-investigation.json`). Owner `codex-my-architecture-serial7`; retain_hot until accepted recovery/superseding evidence, review2026-09-16 is not deletion authority. Canonical main remainsabf77e8 plus prior dirty manuals at this boundary; published runtime branch/main remote arecf5aeff. The registered release worktree holds source plus followup docs; reconcile canonical without erasing prior serial6 notes after the independent release.

# KZ data takeover and Claude operations handoff — 2026-09-09

Owner: codex-my-architecture-serial7. Latest user explicitly assigns the paused Claude KZ data task to Codex; Claude should not resume source/data changes concurrently. This is an operations record, not a message/notification send.

The paused local Claude investigation ended its KZ work at03:05:11Z after9source-read commands and1learning-health GET. No corpus/code writes were observed, and canonical corpus/app/manifest match clean runtime baseabf77e8; existingdirtymanuals were Codex continuity notes. The GET may activate central source metadata; do not repeat it as a strictly read-only probe.

Confirmed corpus:541 immutable legacy resource IDs,512 distinct chapter coordinates,29 duplicate rows. Never delete+renumber IDs or assume text-equality alone identifies duplicate chapters. Title correction alone, with stable keys/digest/count, does not change current KZ admission identity: sourceSha256 is provenance, while current source descriptor uses resourceKey digest/version/count/loader. Existing source completion/progress must remain verifiable. UC and actualsource audit receipts will quantify this boundary.

First independent release repairs source frontend restore: new submenu links do not receive savedread classes, title.includes lookup confuses duplicate entries, and current server progress returns the percentage inmeta. Corpus/manifest bytes and all541 identities stay unchanged in this release. Actual owner20.2 is chapter-540; its previous savedprogress is not lost.

The duplicate-data option under review is512 canonical rows preserving original first-copy IDs, plus29explicitlegacyaliases re-expanding the exact541 compatibilitymanifest. This is NOT yet applied or accepted. It requires correct oldbookmark/reading aliases, loader/builder adaptation and complete source→central→reload acceptance. Central541/163 assessment semantics remain a separately explicit compatibility issue; do not silently change a denominator or rewrite historical student rows.

Evidence: /Users/ylsuen/CF/_meta/reports/operations/.my-architecture-serial7-20260909. Current task matrix remains the complete original My/fleet goal, not only KZ. Further Claude-thread tasks may be taken over only per user's condition after the original goal is completed and Claude quota remains unavailable.
